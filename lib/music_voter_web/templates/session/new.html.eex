<%= form_for @conn, Routes.session_path(@conn, :create), [as: :user], fn f -> %>
  <%= text_input f, :name,
                    placeholder: "Enter your name",
                    required: true,
                    autofocus: true %>

  <%= submit "Submit" %>
<% end %>
